# Nutq 2 scorer runtime deps. torch (CPU-only) is installed separately in the
# Dockerfile via the CPU wheel index to avoid pulling CUDA. Versions are pinned
# to what Phase 0 validated; bump deliberately.
allosaurus==1.0.2
g2p_en==2.1.0
nltk==3.9.1
numpy==1.26.4
aiohttp==3.10.11
