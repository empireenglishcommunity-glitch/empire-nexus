pytest>=7.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.0.0

# Audio quality harness (Empire English Chronicles — see docs/AUDIO-STANDARDS.md).
# The gate must be runnable in CI, so these are dev/CI deps, not bot-runtime deps
# (the 512MB bot container deliberately has no audio engine).
librosa>=0.10.0
soundfile>=0.12.0
pyloudnorm>=0.1.1
