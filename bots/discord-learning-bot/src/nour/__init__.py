"""Aql (العقل) — Nour Intelligence Core (Initiative #15).

The role-scoped, tool-calling, retrieval-grounded cognitive layer that
replaces the single-shot prompt-stuffing pattern in nour_concierge.py.

See .kiro/specs/nour-intelligence-core/ for the full spec
(requirements.md, design.md, tasks.md).

STATUS: Phase A0 (Foundation) — pure scaffolding. No live traffic
routes through this package yet. `nour_concierge.py` remains the live
path until the phased cutover in tasks.md Phase A9 completes.
"""
